if (!hasProperty('mainClass')) {
    ext.mainClass = ''
}

sourceSets {
    main {
        java {
            srcDir 'src/ogre/java'
            srcDir 'src/fbx/java'
            srcDir 'src/xml/java'
        }
    }
    test {
        java {
            srcDir 'src/test/java'
        }
    }
}

dependencies {
    compile project(':jme3-core')
}
